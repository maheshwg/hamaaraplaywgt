# Copy this file to `.env` (DO NOT commit `.env`) and fill in real values.
#
# Local usage:
#   ./create-env.sh
#   # then edit .env
#
# EC2 usage:
#   `deploy-to-ec2.sh` can upload your local `.env` to /opt/youraitester/.env

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=test_automation
DB_USER=testuser
DB_PASSWORD=testpass123

# Application Configuration
SERVER_PORT=8080
# Public URL used in links / callbacks (set to your EC2 public URL in prod)
SERVER_PUBLIC_URL=http://localhost:8080

# OpenAI
OPENAI_API_KEY=sk-your-api-key-here
# Optional
OPENAI_MODEL=gpt-4o

# MCP (Playwright) Configuration
MCP_PLAYWRIGHT_ENABLED=true
MCP_PLAYWRIGHT_SERVER_URL=http://localhost:3000

# Screenshot Storage Configuration: "local" or "s3"
SCREENSHOT_STORAGE_TYPE=local
# When local, backend stores screenshots under `./screenshots` unless overridden:
# SCREENSHOT_LOCAL_DIR=./screenshots

# AWS S3 Configuration (only if SCREENSHOT_STORAGE_TYPE=s3)
AWS_S3_BUCKET=yourqatester-screenshots
AWS_REGION=us-east-1
AWS_ACCESS_KEY=
AWS_SECRET_KEY=
AWS_CLOUDFRONT_URL=

# Email Configuration (for trial signups and notifications)
# Choose ONE method: Microsoft Graph API or SMTP

# Option 1: Microsoft Graph API (for Microsoft 365/Outlook Business - RECOMMENDED)
# Get these from Azure Portal > App Registrations
MICROSOFT_GRAPH_ENABLED=true
MICROSOFT_TENANT_ID=your-tenant-id
MICROSOFT_CLIENT_ID=your-client-id
MICROSOFT_CLIENT_SECRET=your-client-secret

# Option 2: SMTP (for Gmail, SendGrid, etc.)
# Uncomment and use if not using Microsoft Graph
# MICROSOFT_GRAPH_ENABLED=false
# MAIL_HOST=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USERNAME=your-email@gmail.com
# MAIL_PASSWORD=your-app-password

# Email addresses (used by both methods)
MAIL_FROM=support@youraitester.com
TRIAL_SUPPORT_EMAIL=support@youraitester.com


